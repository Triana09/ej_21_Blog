<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Google Analytics -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/add-edit.css" />

    <title>Agregar nuevo usuario</title>
  </head>

  <body>
    <div class="all d-flex flex-column justify-content-center align-items-center">
      <div class="container">
        <div class="card mb-3 shadow">
          <h5 class="card-header">Perfil <%= user.id %></h5>
          <div class="card-body">
            <h2 class="mb-1"><%= `${user.firstname} ${user.lastname}` %></h2>
            <p class="text-muted mt-0"><em><%- user.role.role %> </em></p>

            <div class="list">
                <h5>Mis Artículos:</h4>
              <ul class="list-group">
                <% for(article of articles) {%>
                <li class="list-group-item d-flex align-items-center justify-content-between">
                  <div class="title"><p class="h5 "><%= article.title %></p></div>
                  <div class="action">

                    <a class="text-decoration-none" href="/admin/editar/<%= article.id %>?baseUrl=profile&userId=<%= user.id %>">
                    <button type="button" class="btn btn-outline-secondary">Edit</button>
                    <a href="/admin/editar/<%= article.id %>"></a>

                    <form class="d-inline" action="/admin/eliminar/<%=article.id %>?baseUrl=profile&userId=<%= user.id %>" method="post">
                      <button type="submit" class="btn btn-outline-danger">Delete</button>
                    </form>
                  </div>
                </li>
                <% } %>
              </ul>
            </div>
<div class="mt-4 d-flex flex-row-reverse">
    <button type="button" class="btn btn-outline-warning">Borrar Perfil</button>
</div>
            
          </div>
        </div>
        <a href="/home">Volver a Home</a>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
